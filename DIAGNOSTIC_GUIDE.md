# Диагностика проблем с непрочитанными сообщениями и WebSocket

## Быстрая проверка

### 1. Откройте тестовую страницу
```
http://localhost:3000/test-unread
```

### 2. Проверьте интеграцию
✅ **Что должно работать:**
- Хук `useUnreadMessages` теперь интегрирован с `useChats` и `useMessages`
- При добавлении тестовых сообщений счётчик должен увеличиваться
- При пометке как "прочитанное" счётчик обнуляется
- WebSocket статус отображается в реальном времени

### 3. Проверьте WebSocket подключение
- **Статус:** Должен показывать "connected" если всё работает
- **Токен:** Должен показывать "Есть" если пользователь авторизован
- Если токена нет - сначала авторизуйтесь в приложении

## Возможные проблемы и решения

### Проблема 1: WebSocket не подключается
**Симптомы:** Статус показывает "disconnected" или "error"

**Решения:**
1. Проверьте токен авторизации:
   ```javascript
   localStorage.getItem('auth_token')
   ```

2. Проверьте доступность сервера:
   ```
   wss://socket.eldor.kz/api/ws
   ```

3. Откройте консоль браузера - должны быть логи WebSocket

### Проблема 2: Непрочитанные сообщения не обновляются
**Симптомы:** Бейджи не появляются в сайдбаре

**Решения:**
1. Убедитесь что используется обновлённая версия `useChats`
2. Проверьте что WebSocket подключён
3. В тестовой странице проверьте работу `useUnreadMessages`

### Проблема 3: Счётчики не сбрасываются при переключении чатов
**Симптомы:** Бейджи остаются после открытия чата

**Решения:**
1. Проверьте что в `page.tsx` вызывается `markChatAsRead()`
2. Убедитесь что интеграция с `useUnreadMessages` работает

## Пошаговая диагностика

### Шаг 1: Базовая функциональность
```bash
# Откройте тестовую страницу
http://localhost:3000/test-unread

# Нажмите "Добавить 1 сообщение"
# Счётчик должен увеличиться на 1

# Нажмите "Пометить как прочитанное"  
# Счётчик должен стать 0
```

### Шаг 2: WebSocket диагностика
```bash
# На тестовой странице проверьте:
# - Статус WebSocket (connected/disconnected)
# - Наличие токена авторизации
# - Попробуйте "Тест WebSocket сообщения"
```

### Шаг 3: Реальное приложение
```bash
# Откройте основное приложение
http://localhost:3000

# Авторизуйтесь если нужно
# Переключайтесь между чатами
# Бейджи должны появляться/исчезать
```

## Логи для отладки

### В консоли браузера должны быть:
```
✅ WebSocket успешно подключен!
📤 Отправлен ping
📨 WebSocket message received: {...}
```

### Если WebSocket не работает:
```
❌ Токен авторизации не найден - WebSocket ожидает авторизации
❌ Ошибка WebSocket: ...
🔄 Планируется переподключение WebSocket через 5000 ms
```

## Файлы для проверки изменений

### `src/hooks/useChats.ts`
- Должен импортировать `useUnreadMessages`  
- В `handleNewMessage` должен вызывать `addUnreadMessage`
- В `markChatAsRead` должен вызывать `markUnreadChatAsRead`
- В `loadChats` должен обновлять `unread` через `getUnreadCount`

### `src/hooks/useMessages.ts`  
- Должен импортировать `useUnreadMessages`
- В `handleNewMessage` должен добавлять непрочитанные для чужих чатов

### `src/app/page.tsx`
- Должен вызывать `markChatAsRead(chatId)` при смене чата

## Если ничего не помогает

1. **Перезапустите сервер разработки**
2. **Очистите localStorage:**
   ```javascript
   localStorage.clear()
   ```
3. **Проверьте сетевые запросы в DevTools**
4. **Посмотрите логи в консоли браузера**